apiVersion: v1
kind: ConfigMap
metadata:
  name: focusflow-config
  namespace: cloudpath
  labels:
    app.kubernetes.io/name: focusflow-api
    app.kubernetes.io/part-of: cloudpath-focusflow
    app.kubernetes.io/component: configuration
data:
  # Application settings
  NODE_ENV: production
  APP_NAME: focusflow-api
  APP_VERSION: "1.1.0"
  RELEASE_MESSAGE: "FocusFlow full-stack release 1.1.0"
  LOG_LEVEL: info
  
  # Server settings
  PORT: "5000"
  HOST: "0.0.0.0"

  # CORS settings (Meka aluthen add karanna)
  CORS_ORIGIN: "http://localhost:8080"