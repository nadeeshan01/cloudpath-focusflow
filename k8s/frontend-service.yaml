apiVersion: v1
kind: Service
metadata:
  name: focusflow-web
  namespace: cloudpath
spec:
  selector:
    app.kubernetes.io/name: focusflow-web
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP