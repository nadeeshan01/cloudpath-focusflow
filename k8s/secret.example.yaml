# ⚠️ THIS IS AN EXAMPLE ONLY
# DO NOT commit real secrets to Git
# Copy this to secret.local.yaml and add real values

apiVersion: v1
kind: Secret
metadata:
  name: focusflow-secret
  namespace: cloudpath
  labels:
    app.kubernetes.io/name: focusflow-api
    app.kubernetes.io/part-of: cloudpath-focusflow
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Database (replace with real value)
  DATABASE_URL: "mongodb://placeholder:27017/focusflow"
  
  # JWT Secret (replace with real value)
  JWT_SECRET: "replace-with-real-value-outside-git"
  
  # API Keys (add as needed)
  # AWS_ACCESS_KEY_ID: "replace-me"
  # AWS_SECRET_ACCESS_KEY: "replace-me"